# Portfolio — Environment Variables

# MongoDB connection string (required)
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.vo9nqo4.mongodb.net/portfolio?appName=Cluster0

# Server port (optional, defaults to 3000)
PORT=3000
